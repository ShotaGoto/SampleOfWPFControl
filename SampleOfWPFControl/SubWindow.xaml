<Window x:Class="SampleOfWPFControl.SubWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"

        Title="SubWindow" Height="450" Width="800">
    <Grid>
        <StackPanel>
            <DataGrid Name="DG4" ItemsSource="{Binding}" MinHeight="400" Height="Auto" AutoGenerateColumns="False" CanUserAddRows="False" AllowDrop="True" MouseMove="DG4_MouseMove" Drop="DG4_Drop" >
                <DataGrid.Columns>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" IsReadOnly="True" />
                    <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</Window>
